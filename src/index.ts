console.log(' decorator in TypeScript')

// Descriptors are attributes that we give to classes and members, thus changing their behavior.
// A decorator is actually a function that is called by the JavaScript runtime.

@Component
class ProfileComponent {

}
